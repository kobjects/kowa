package org.kobjects.kowa.core.gc

import org.kobjects.kowa.binary.WasmWriter
import org.kobjects.kowa.core.type.Type

class Arr(type: Type) : Type {
    override fun toWasm(writer: WasmWriter) {
        TODO("Not yet implemented")
    }
}